#include <stdio.h>
int add();

int main(int argc, const char *argv[]){
    int x,y;
    add(x,y);
    return 0;
}

int add(int param1,param2) {
    int a,b;
    scanf("%d%d",&a,&b);
    int sum = 0;
    sum = a+b;
    printf("%d",sum);
}
